let destinations: string[] = ["Kyoto, Japan", "Reykjavik, Iceland", "Cape Town, South Africa", "Buenos Aires, Argentina", "Cairo, Egypt"];

// Previous operations have been performed without altering the original array.

// Show the original array is still in its original order by printing it again.
console.log("Original order remains:", destinations);
