let destinations6: string[] = ["Kyoto, Japan", "Reykjavik, Iceland", "Cape Town, South Africa", "Buenos Aires, Argentina", "Cairo, Egypt"];

// Reverse the order of the list
destinations6.reverse();

// Print the array to show that its order has changed
console.log("Reversed order:", destinations6);
